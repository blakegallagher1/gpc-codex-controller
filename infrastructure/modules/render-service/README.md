# Optional Render Module

This module is optional and only used when `enable_render = true`.

Inputs required:
- `api_key`
- `owner_id`
- `service_name`
- `repo_url`
- `repo_branch`
- `plan`

The module configures a Render web service that builds and starts the `gpc-codex-controller` repo.
